program FreeCAD;

uses
  System.StartUpCopy,
  FMX.Forms,
  f_Main in 'f_Main.pas' {Form3},
  f_Simulation in 'f_Simulation.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm3, Form3);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
